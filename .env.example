# Universal AI Gateway — Environment Example (do not commit real secrets)
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
ALLOWED_API_KEYS=['default', '1234', 'please_change_these_values'] # CSV; required in non-dev
CEREBRAS_API_KEY=
CEREBRAS_BASE_URL=
OLLAMA_HOST=http://localhost:11434
# Optional; used by the Ollama provider and endpoints. Set to your Ollama server base URL.
# Example: http://localhost:11434 or http://ollama:11434 in Docker/K8s
REQUEST_TIMEOUT_S=30
ENABLE_SECURITY_HEADERS=true
# ENABLE_ENRICHMENT=false  # Single toggle for opt-in enrichment hints; default False keeps router-first behavior with no changes
DEVELOPMENT_MODE=true  # Set to true for development mode; enables all API keys and skips auth checks if REQUIRE_AUTH is false
REQUIRE_AUTH=true  # Set to false so development mode skips auth checks
